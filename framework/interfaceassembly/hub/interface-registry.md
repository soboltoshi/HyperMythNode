# Interface Registry

**What it is:** The shared-contract layer for Interface Assembly.

## What it does

- keeps reusable contracts outside local box prose
- decides when a connection should be promoted to shared status
- keeps adapters visible as edges instead of state owners

## When to use it

Use this when the same contract is appearing in more than one box or when a local connection starts behaving like shared infrastructure.
