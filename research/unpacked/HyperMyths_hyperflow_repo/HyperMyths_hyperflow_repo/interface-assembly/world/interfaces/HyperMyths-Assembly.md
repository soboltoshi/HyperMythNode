# HyperMyths Assembly

All HyperMyths blocks must be classified as box, interface, adapter, client, worker, rail, vault, or index.

Shared contracts belong in `packages/protocol`.
AI routing belongs in `packages/ai-gateway`.
Adapters must never own truth.
